<!DOCTYPE html>
<html>
<head>
<script>
    var winWidth = 800;
    var winHeight = 450;
    window.resizeTo(winWidth, winHeight);
    window.moveTo(screen.width / 2 - winWidth / 2, screen.height / 2 - winHeight / 2);
</script>
    <title>Preview</title>
    <meta charset="UTF-8">
    <script src="zero.js"></script>
    <style>
body {
    margin: 5px;
}
.cntr {
  text-align: center;
  padding: 20px;
  font-size: 20px;
}
a {
  text-decoration: none;
  padding: 5px 20px;
  background-color: #ffffff;
  color: #0099e3;
  transition: all 0.3s ease;
}
a:hover {
  background-color: #ccffcc;
  color: #0099e3;
}
a:visited {
  color: #0099e3;
}
textarea {
  width: 80%;
  height: 300px;
  position: relative;
  margin-bottom: 10px;
}
    </style>
</head>

<body>

<div class="cntr"><textarea>function findWord(word, string) {
	var instancesOfWord = string.match(new RegExp('\b'+word+'\b', 'ig'));
	alert(instancesOfWord);
}
findWord('car', 'Carl went to buy a car but had forgotten his credit card.');</textarea><br><a href="#" onclick="window.location.reload();">Script Reload</a></div><script>
function go(){function findWord(word, string) {
	var instancesOfWord = string.match(new RegExp('\b'+word+'\b', 'ig'));
	alert(instancesOfWord);
}
findWord('car', 'Carl went to buy a car but had forgotten his credit card.');
}
setTimeout(function() {go();}, 100);
</script>
</body>

</html>